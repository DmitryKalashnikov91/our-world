import LINKS from '../../constants/Links';
import styles from '';

const MobileMenu = () => {
    return (
        <nav>
            <ul>
                <li></li>
            </ul>
        </nav>
    );
};

export default MobileMenu;
